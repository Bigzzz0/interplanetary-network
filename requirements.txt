# Interplanetary-Style Low-Latency Multi-Sensory Network
# Dependencies for all components

# Core FastAPI and ASGI server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Computer Vision
opencv-python>=4.9.0
opencv-contrib-python>=4.9.0

# Cryptography for Ed25519 signing
pynacl>=1.5.0

# Additional utilities
python-multipart>=0.0.6
websockets>=12.0
numpy>=1.26.0
aiofiles>=23.2.1

# ===========================================
# NEW: Video/Audio Support Dependencies
# ===========================================

# Video Encoding/Decoding
av>=10.0.0  # PyAV for H.264/MJPEG encoding
pillow>=10.0.0  # Image processing

# Audio Processing
sounddevice>=0.4.6  # Audio capture
soundfile>=0.12.1  # Audio file handling

# ML Framework for Frame Interpolation
torch>=2.1.0
torchvision>=0.16.0

# RAFT Optical Flow (for ML-based interpolation)
einops>=0.7.0  # For RAFT model
# correlation-cuda>=0.1.0  # CUDA correlation for RAFT (optional, GPU only) - removed due to install issues

# DAIN Frame Interpolation (pre-trained model support)
scipy>=1.11.0  # For DAIN model utilities

# Dataset utilities (for loading pre-recorded video)
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
